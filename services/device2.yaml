# Murano Product Spec File - Smart Lighbulb Example Application

protocol:
  name: onep
provisioning:
  auth_type: token

resources:
  temperature:
    format: number
  humidity:
    format: number
  state:
    format: number
    initial: 0
  uptime:
    format: number
